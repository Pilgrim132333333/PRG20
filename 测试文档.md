# 本地运行步骤（简版）

## 1. 数据库（SQL）

1. **启动 MySQL**（本机服务已运行即可）。
2. **建库**（库名与项目根目录 `.env` 里 `DB_NAME` 一致，默认示例可为 `pga_platform`）：
   ```sql
   CREATE DATABASE IF NOT EXISTS pga_platform CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;
   USE pga_platform;
   ```
3. **执行建表脚本**（在项目根目录或 `backend` 下，路径按你本机调整）：
   ```bash
   mysql -u 你的用户 -p pga_platform < backend/init_schema.sql
   ```
4. **（可选）导入示例数据**：配置好项目根目录 `.env` 中的 `DB_HOST`、`DB_PORT`、`DB_USER`、`DB_PASSWORD`、`DB_NAME` 后，在 `backend` 目录执行：
   ```bash
   python seed_data.py
   ```

---

## 2. 开启后台（FastAPI）

1. 进入后端目录并安装依赖（首次）：
   ```bash
   cd fastapi-backend
   pip install -r requirements.txt
   ```
2. 确认数据库：在 `fastapi-backend` 下执行 `python seed_data.py` and `python check_db.py`，看到 **OK** 再继续。
3. 启动服务（默认 `8000` 端口）：
   ```bash
   uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
   ```
4. 浏览器访问 `http://127.0.0.1:8000/` 若返回 JSON，说明后台已起来。

---

## 3. 访问网站（前端）

- **方式 A**：用浏览器**直接打开**静态页（路径按你本机）  
  `vue-frontend/index.html`（首页）→ 登录/注册 → `student.html`。
- **方式 B**：在 `vue-frontend` 目录起一个本地静态服务（避免部分浏览器对 `file://` 的限制），例如：
  ```bash
  cd vue-frontend
  python -m http.server 8080
  ```
  然后浏览器打开：`http://127.0.0.1:8080/index.html`。

前端里接口地址一般为 `http://127.0.0.1:8000/api`，请与后台端口一致。
